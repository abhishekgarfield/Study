import {SafeAreaView, Text, View} from 'react-native';
import MainHeader from '../Common/headers';

const Stat = () => {
  return (
    <SafeAreaView>
      <MainHeader />
    </SafeAreaView>
  );
};

export default Stat;
